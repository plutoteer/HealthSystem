## 健康管理系统中期报告（非运动模块）

---

### 一、项目目标与阶段性成果

本项目面向在校学生健康服务场景，围绕“体检管理、医保管理、AI问询、用户与权限管理”构建一体化平台。中期阶段已完成核心业务链路闭环：

1. **体检管理双端化**：管理员发布体检场次，用户侧独立页面预约并查看记录。  
2. **医保管理双端化**：管理员发布医保项目并查看用户参保缴费，用户侧查看医保项目并完成报名缴费。  
3. **AI问询系统升级**：从单轮关键词问答升级为“结构化问询 + 规则分诊 + 政策知识库导航 + Dify增强解读”。  
4. **菜单权限精细化**：基于角色-菜单关系实现管理员与普通用户不同入口和不同操作权限。

---

### 二、功能需求分析（非运动模块）

### 2.1 用户与权限管理需求

#### 需求目标
- 支持用户登录、注册、获取用户信息。
- 按角色动态加载菜单，管理员与普通用户功能隔离。

#### 已实现
- 登录/退出登录、用户信息获取、动态路由注入。
- 菜单来自数据库（`j_menu` + `j_role_menu`），前端按角色渲染。

---

## 2.2 体检管理需求

### 需求目标
1. 管理员可发布体检场次（地区、体检时间）、修改和删除。  
2. 用户可查看可预约场次并发起预约。  
3. 管理员可查看用户预约记录。

### 已实现
- **管理员端页面**：`sys/ExamAppointmentManage`  
  - 体检场次发布/编辑/删除/分页查询  
  - 用户预约记录分页查询
- **用户端页面**：`user/UserExamAppointment`  
  - 查看管理员发布场次  
  - 一键预约  
  - 查看“我的预约记录”

---

## 2.3 医保管理需求

### 需求目标
1. 管理员可发布医保项目（年度、说明、金额、时间窗口、状态）。  
2. 用户可查看医保项目，提交参保信息并完成缴费。  
3. 管理员可查看用户参保缴费信息。

### 已实现
- **管理员端页面（更名）**：`sys/InsuranceManage`（用户医保信息管理）  
  - 医保信息发布（CRUD + 分页）  
  - 用户参保缴费信息查看（分页筛选）
- **用户端页面**：`user/UserInsuranceManage`（医保信息管理）  
  - 查看可参保项目  
  - 参保报名与缴费提交  
  - 查看“我的参保缴费记录”
- 后端校验参保时间窗口与状态（未到开始时间、超过截止时间、项目关闭会被拦截）。

---

## 2.4 AI问询系统需求

### 需求目标
1. 支持健康问题咨询与政策咨询。  
2. 症状咨询可进行多轮结构化引导并给出分级建议。  
3. 政策咨询可定位政策知识库内容并给出可读解读。  
4. 保障医疗场景可控性，避免无依据回答。

### 已实现
- 单轮兼容接口：`/ai/consult`。  
- 多轮会话接口：`/ai/chat/start`、`/ai/chat/message`。  
- 意图路由：`SYMPTOM_TRIAGE / POLICY_QUERY / GENERAL_QA`。  
- 数据驱动分诊规则：槽位模板、红旗词典、分诊规则、建议模板。  
- Dify 增强政策解读（失败自动降级规则答复）。

---

## 三、前后端技术栈

## 3.1 后端技术栈

- **语言/框架**：Java + Spring Boot
- **ORM**：MyBatis-Plus
- **数据库**：MySQL
- **缓存/会话辅助**：Redis（项目已配置）
- **接口风格**：RESTful API
- **统一响应**：`Unification<T>`

### 后端模块划分
- `controller`：接口层（如 AI、医保、体检）
- `service`：业务逻辑层（规则、校验、流程编排）
- `mapper`：数据访问层（MyBatis-Plus）
- `entity`：实体映射（DB表结构映射）

---

## 3.2 前端技术栈

- **框架**：Vue2
- **UI组件库**：Element UI
- **路由**：Vue Router（动态路由注入）
- **状态管理**：Vuex
- **请求库**：Axios（统一请求拦截与响应处理）

### 前端工程特点
- 菜单动态渲染（后端返回 `menuList`）。
- 页面以管理台风格为主，使用表格、分页、弹窗、标签页组织信息。

---

## 四、页面风格与渲染实现

## 4.1 统一管理台风格

1. 使用 `el-card + el-table + el-pagination` 形成“查询-列表-分页”标准布局。  
2. 对新增/编辑操作采用 `el-dialog + el-form`，保证交互一致性。  
3. 操作按钮统一采用语义色：主操作蓝色、删除红色。  
4. 使用 `v-loading` 展示异步请求状态，提升反馈质量。

## 4.2 双端页面渲染风格差异

- **管理员页面**偏“数据管理/发布”：更强调筛选、批量查看、信息完整度。  
- **用户页面**偏“服务办理”：更强调流程清晰（查看 -> 提交 -> 结果反馈）。

## 4.3 渲染性能与体验

- 分页查询减少一次性渲染压力。  
- 表格列按业务优先级布局，关键字段前置。  
- 时间、状态字段统一格式化显示（便于识别）。

---

## 五、AI问询系统具体实现

## 5.1 总体架构

AI问询采用“**规则引擎可控 + 知识库增强**”混合架构：

\[
\text{Intent Router} + \text{Slot Filling} + \text{Rule Engine} + \text{Policy KB/RAG} + \text{Fallback}
\]

### 核心思想
- 高风险医疗建议必须可控，优先规则。  
- 政策解读需要自然语言质量，结合 Dify 做增强。  
- 模型失败时自动降级，保证服务连续性。

---

## 5.2 关键实现流程

### A. 会话与意图路由
- 用户通过 `/ai/chat/start` 获取 `sessionId`。  
- 每轮消息经 `/ai/chat/message` 进入后端。  
- 根据关键词与上下文识别当前意图：
  - `POLICY_QUERY`：医保政策类
  - `SYMPTOM_TRIAGE`：症状分诊类
  - `GENERAL_QA`：普通问答

### B. 结构化问询（症状分诊）
- 从 `ai_symptom_slot_template` 读取必填槽位（部位、性质、时长、严重度、伴随症状等）。  
- 槽位未补齐时按模板追问。  
- 命中红旗词典（`ai_red_flag_dict`）时优先高风险输出。  
- 未命中红旗则按 `ai_triage_rule.condition_json` 判定 L1/L2/L3。  
- 最终建议从 `ai_advice_template` 获取标准文案。

### C. 政策知识库导航
- 先用本地政策知识（`j_ai_qa` / 政策问答）做基础匹配。  
- 再由 Dify 流程（知识检索 + LLM）输出“结论/条件/办理建议”。  
- 返回 `provider` 字段区分来源：
  - `hybrid`：知识库 + 模型增强
  - `rule`：降级规则回答

### D. 安全与兜底
- 统一免责声明：仅供参考，不替代医生诊断。  
- 高风险症状优先线下就医建议。  
- 外部服务不可用时自动回退本地规则。

---

## 5.3 AI模块优势

1. **可控性强**：分诊规则在数据库维护，避免模型“自由发挥”。  
2. **可解释性强**：可追踪命中的红旗词条、规则与建议模板。  
3. **可维护性强**：策略与文案可通过配置/数据表更新。  
4. **稳定性高**：模型调用失败可降级，服务不中断。  
5. **扩展性好**：后续可平滑接入向量检索、重排序、更多模型。

---

## 六、数据库设计（非运动模块）

## 6.1 体检相关
- `j_physical_exam_slot`：管理员发布体检场次  
- `j_physical_exam_appointment`：用户预约记录

## 6.2 医保相关
- `j_insurance_info`：医保项目信息（年度、金额、时间窗口、状态）  
- `j_insurance_enrollment`：用户参保缴费记录

## 6.3 AI相关
- `j_ai_qa`：问答知识库  
- `ai_symptom_slot_template`：结构化问询槽位模板  
- `ai_red_flag_dict`：红旗症状词典  
- `ai_triage_rule`：分诊规则  
- `ai_advice_template`：建议模板

## 6.4 权限相关
- `j_menu`：菜单配置  
- `j_role_menu`：角色菜单授权  
- `j_user_role`：用户角色绑定

---

## 七、阶段问题与优化方向

## 7.1 已解决问题
1. 会话意图“锁死”导致政策问题误走分诊的问题已修复。  
2. 管理端/用户端菜单串权限问题通过角色绑定清理解决。  
3. Dify 调用失败时的超时与降级逻辑已完善。

## 7.2 后续优化
1. 会话状态从内存迁移到数据库持久化。  
2. 政策知识库引用从示例信息升级为条款级引用。  
3. 引入向量检索与重排序提升政策问答召回准确率。  
4. 增加审计日志与可观测指标（命中规则、模型耗时、失败率）。

---

## 八、中期结论

当前系统已完成非运动模块核心能力建设：

- 体检管理与医保管理均实现“管理员发布 + 用户办理/预约 + 记录可追踪”的完整闭环；  
- AI问询系统完成从基础问答到结构化分诊与政策导航的升级；  
- 前后端分层清晰，角色权限与动态菜单机制稳定可用。

整体进展达到中期目标，为后续“精度优化、审计强化、知识库深度增强”打下了可落地基础。
